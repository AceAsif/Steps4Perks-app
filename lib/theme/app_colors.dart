import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4DD0E1); // Teal blue used in the arc and bottom nav
  static const Color secondary = Color(0xFF26C6DA); // Slightly deeper teal (optional use)
  static const Color background = Color(0xFFE7FAFA); // Light cyan background
  static const Color textPrimary = Color(0xFF212121); // Nearly black
  static const Color textSecondary = Color(0xFF757575); // Grey text
  static const Color streakFlame = Color(0xFFFF5722); // Flame icon orange
  static const Color buttonGradientStart = Color(0xFFFF7043); // Left side of redeem button
  static const Color buttonGradientEnd = Color(0xFFFFA726); // Right side of redeem button
  static const Color cardBackground = Colors.white; // Card white background
}